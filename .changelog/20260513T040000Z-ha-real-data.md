---
category: Fixed
---

- `services/health-aggregator`: `/services/{name}` detail endpoint now returns real CloudWatch Logs entries and real Container Insights CPU/memory metrics instead of zeroed/empty stubs. Added `CloudWatchClient` for ECS Container Insights (`CpuUtilized`, `CpuReserved`, `MemoryUtilized`, `MemoryReserved`); expanded `LogsClient` with `get_recent_events` and `get_recent_errors`; added `detail_for()` to `HealthAggregator` fanning out all probes in parallel.
- `services/health-aggregator`: added `@app.options("/services/{name}")` CORS preflight handler so the SPA detail panel no longer gets a 403 from API Gateway.
- `services/health-aggregator`: boto3 clients for CloudWatch Metrics now share the same `connect_timeout=5 / read_timeout=10` config used by ECS/ELBv2/Logs.
- `services/health-aggregator/registry.py`: restored all 13 services (including undeployed ones); undeployed services correctly surface as `unknown / ecs service not found`.
- `services/admin`: removed `(mocked)` labels from Resource Usage, Recent Log Entries, and Recent Errors card titles; added N/A display when Container Insights has no data yet.
- `infra/dev/iam`: added `cloudwatch:GetMetricData` to the health-aggregator task role so Container Insights queries succeed.
