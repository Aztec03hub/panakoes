---
category: Added
---

- `infra/dev/kms`: new Terraform module provisioning 2 consolidated CMKs (`alias/panakoes/app-data` and `alias/panakoes/logs`) as the first stage of Wave 2 KMS consolidation. The keys are created but not yet wired into any consumer; follow-up PRs migrate S3 buckets, RDS, Secrets Manager, SQS, SNS, ECR, Backup vault, and CloudWatch log groups onto them. JWT signing (`alias/panakoes-dev-jwt-signing`) and Terraform state (`alias/panakoes-tf-state`) keys stay separate for security and bootstrap reasons. Cost impact at this stage: +$2/mo gross (2 new keys at $1/mo each); net -$13/mo gross once W2-T2..T7 retire the 15 old keys.
