---
category: Changed
---

- `services/{auth,admin,otel-lib-ts}`: executed 7 of the 10 RISKY-MAJOR Dependabot PRs flagged in `docs/dependabot-triage-2026-05-11.md`. Shipped: @types/node 22→25 across all three TS services (#248, #256, #261, type-only, typecheck/svelte-check clean); @biomejs/biome 1→2 in otel-lib-ts (#252) and admin (#259) with `biome migrate --write` config-schema rewrite + safe organize-imports/export-name-sort autofixes (admin also excludes `src/app.css` from biome's file scope because biome 2's CSS parser does not understand Tailwind v3 `@tailwind`/`@apply` directives; revisit when admin migrates to Tailwind v4); typescript 5→6 in auth (#251, tsc --noEmit clean); vitest 2→4 in auth (#253, bundled #255 coverage-v8 4.1.6 as the peer-required pair, rewrote `poolOptions.forks.singleFork: true` to top-level `maxWorkers: 1, isolate: false` per upstream v4 migration guide); vitest 2→4 in otel-lib-ts (#254, bundled #250 coverage-v8 4.1.6, added explicit `vite@6` devDep because vitest 4 needs vite ≥ 6 for the `./module-runner` subpath export and previously resolved transitively to vite 5 which broke vitest startup; 21/21 tests pass locally). Deferred: vite 6→8 in admin (#260) because the upgrade chain requires bumping `@sveltejs/vite-plugin-svelte` past 4.x first (vite 8 ships rolldown@1.0.0 which 4.x is incompatible with) so a coordinated SvelteKit upgrade PR is the right unit; tailwind-variants 0.3→3.2 in admin (#258) because 3.x peer-requires Tailwind CSS v4 and admin is still on Tailwind v3.
