---
category: Fixed
---

- `infra/dev/ecs`: fix `Unclosed configuration block` errors in `outputs.tf` (duplicate `nlb_listener_arns` body) and `variables.tf` (5 ingestion_api/summarization variable pairs missing closing braces). Damage landed via PR #229's squash-merge where mechanical sed-strip conflict resolution dropped block closures. Restores `terraform fmt|validate|plan` so infra PRs unblock.
