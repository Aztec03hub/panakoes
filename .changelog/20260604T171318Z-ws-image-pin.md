---
category: Fixed
---

- `infra/api-gateway-ws`: streaming-router and ws-authorizer image tags pinned to known-good builds; the `latest` defaults let the apply-on-merge CI roll both Lambdas back to stale images, 500ing every WebSocket connect.
