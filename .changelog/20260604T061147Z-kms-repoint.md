---
category: Changed
---

- `infra/dev/kms`: codified the live CloudFront service-use statement on the consolidated `alias/panakoes/app-data` key policy.
- `infra/dev/transcribe-worker`: re-pointed the `transcribe-trigger` SQS queue + DLQ to `alias/panakoes/app-data` and the worker Lambda log group to `alias/panakoes/logs`.
- `infra/dev/api-gateway`: re-pointed the `api-index` Lambda log group to `alias/panakoes/logs` and removed its per-service log CMK + alias.
