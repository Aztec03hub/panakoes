---
category: Fixed
---

- `infra/dev/ecs`: fix health_aggregator_health_check_path default from /health (auth-gated, returns 401) to /healthz (public liveness probe). Prevents the task definition container healthcheck from crash-looping.
