---
category: Fixed
---

- `services/admin`: file-upload transcription no longer ends the session before the GPU finishes catch-up; the drain grace now waits for the session to reach `transcribing` (first live e2e ended 7 minutes early and discarded the whole recording).
