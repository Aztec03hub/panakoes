---
category: Changed
---

- `docs/aws_activate_application.md` + memory `aws_account_panakoes.md`: added a status-as-of-2026-05-11 verification of the AWS Activate Founders application state for the LaFayette Labs / Panakoes account `659225405128`. Captures that the application is PENDING (re-submitted 2026-05-09 after the root-account email moved to `phil@lafayettelabs.com`, AWS approval window 7-14 days, no email yet at `plafaydev@gmail.com` via Cloudflare forwarding); that credit balance is not exposed via any AWS CLI / SDK call (`aws ce get-cost-and-usage` shows gross UnblendedCost only and the May 1-11 burn is effectively $0; `aws support describe-services` returns `SubscriptionRequiredException` against Basic Support, which is identical pre- and post-Founders since Founders bundles no paid support plan); and documents the canonical Billing-console URL `https://console.aws.amazon.com/billing/home#/credits` as the single source of truth Phil clicks to verify. Documents the operator workflow for credit application (auto-applies at monthly invoice generation, Cost Explorer continues to show gross numbers, net-of-credit visible only in Billing console → Bills), the Activate Portfolio follow-up tier (not applicable yet; requires accelerator affiliation or ~$50k+ outside funding; revisit if a wearable pre-seed round, accelerator acceptance, or notable angel materializes), and the re-application schedule (credits expire 12 months after issuance; calendar reminder at approval + 11 months, target 2027-04-09 assuming approval lands within the standard window; re-application allowed if prior credit was exhausted or expired, rejected as duplicate if significant unused balance remains). Memory mirrors the same fields as a `## Activate Founders status` section beneath the existing account identity block.
