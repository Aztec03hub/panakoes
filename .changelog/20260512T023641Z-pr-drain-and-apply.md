---
category: Changed
---

- PR queue drain (2026-05-11T23:33Z): rebased 20 DIRTY/BEHIND PRs (#227, #229, #230, #243, #244, #246, #263, #264, #265, #266, #267, #269, #271, #272, #273, #277, #278, #279, #281, #282, #283) against `origin/main` in tier dependency order, force-pushed each with `--force-with-lease`, and re-armed `gh pr merge --auto`. All conflicts were mechanical CHANGELOG / index-table conflicts in additive list files; resolved via the keep-both-sides `sed` pattern documented under "Mechanical conflict resolution patterns" in `CLAUDE.md`. The safe-list was extended at run time to cover `docs/runbooks/README.md` and `docs/adr/README.md` (both are ID-tabled index files of independent rows, same shape as `infra/README.md`); a follow-up PR to codify these in `.gitattributes` with `merge=union` is recommended. Dependabot DIRTY PRs (#259, #260, #261) intentionally left alone per PR #262 triage (RISKY-MAJOR: biome 1 to 2, vite 6 to 8, @types/node 22 to 25). Phase B terraform apply cascade was deferred: at run end only PR #265 (SES) had merged, with the remaining 18 PRs queued through CI; applying against pre-merge `main` would have been no-op at best and drift-creating at worst. Run report at `.agent-runs/2026-05-11T23-33-45Z-pr-drain-and-apply.md` captures the full per-PR ledger, the orphan-process sighting from a parallel session (`security/iam-resource-star-audit`), and the rollback procedure.
