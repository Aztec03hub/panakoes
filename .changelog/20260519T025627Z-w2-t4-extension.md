---
category: Changed
---

- `infra/dev`: extend W2-T4 CMK consolidation to `api-gateway-ws` (3 log groups), `step-functions` (state-machine log group), and `cost-rollup-aggregator` (Lambda log group); fix latent step-functions output-name bug (`cloudwatch_logs_kms_key_arn` -> direct `logs_key_arn` lookup); module-local CMK resources retained for W2-T7 retirement.
