---
category: Changed
---

- `docs/design/tool-trace-telemetry.md`: apply Gate 2 adversarial review findings (3 CRIT + 4 top HIGH + 4 other HIGH/MED) and add Section 4.7 forking disler's project to Panakoes-owned GitHub at pinned SHA `8a6e5cf795df50767cea7123703751282a819697`. Key changes: trace propagation mechanism spec'd end-to-end in new Section 3.5 subsection (flusher generates/caches trace_id per session, maintains active-span map, links pre/post via shared span_id); rotation SQL fixed (`t` -> `timestamp`); PR-number extracted at hook-time into dedicated `pr_number` column (preserves Section 8 no-response-body invariant); hook latency budget relaxed from 5 ms to 15 ms p99 warm with collapsed-jq mitigation and Python escape hatch; shim uses `mktemp` for collision-free filenames; `auto_vacuum = INCREMENTAL` + dropped manual VACUUM removes rotation-stall risk; dedup_key UNIQUE constraint + `INSERT OR IGNORE` protects against double-ingestion; multi-machine EC2 deferred to v2; v1 explicit localhost + Linux/WSL2 only; setup script scope cut (prereq-check Bun, drop Vite dev server, print URL not browser-open). Design grows from 624 to 732 lines.
