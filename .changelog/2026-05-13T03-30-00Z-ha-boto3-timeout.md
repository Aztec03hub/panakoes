### Fixed
- Added explicit boto3 connect (5s) and read (10s) timeouts to health-aggregator ECS, ELBv2, and CloudWatch Logs clients. Without these, a blocked or slow AWS API call would hang for the default 60-second socket timeout, silently consuming the APIGW 29-second integration budget and returning 503 to the caller.