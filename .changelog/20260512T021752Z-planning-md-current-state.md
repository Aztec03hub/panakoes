---
category: Changed
---

- `PLANNING.md`: refreshed the running ADR journal and current-state inventory through 2026-05-11 wave 5. Decision register now indexes file-form ADRs through ADR-040 (with forward references to ADR-041 RS256+KMS+JWKS migration path and ADR-042 MFA step-up deferral). Added five new ADR journal entries (ADR-038 api-gateway proxy + overrides routing; ADR-039 auth-db split-credential + operator-invoked migration runner; ADR-040 tenant cost rollup service dimension; ADR-041 KMS-backed JWKS plan; ADR-042 step-up MFA deferred until first billing-mutation route). Added current-state Services table (24 services with code + deploy state including auth deployed at `migrate-018532f`, cost-api / admin-api / cost-rollup-aggregator / event-router deployed, ingestion-api / query-api / step-functions / batch / frontend TF-defined-not-applied, summarization / notification / session-manager / billing / health-aggregator / gpu-spawner / transcribe-worker in flight) and Infrastructure inventory table (22 modules with apply state including the `infra/dev/api-gateway/` partial-applied state and 7 placeholder secrets pending real-value writes). Closed six open decisions (api-gateway routing shape, auth-db credential scope, migration invocation timing, tenant cost rollup schema, JWT slice-2 implementation path, when step-up MFA becomes a hard requirement). Added a Failure modes index pointing at 15 memory files captured this session (pre-push SSH idle timeout, `tail -N` background buffering, Edit-requires-Read, gitignore build artifacts, verify branch before commit, sync main before terraform plan, AWS-semantic validation gaps, ECS Fargate first-deploy checklist, Lambda container image gotchas, CE anomaly EMAIL requires DAILY, Lambda log group dedicated CMK pattern, api-gateway partial apply, ci-local-first, idle-time-is-failure). Added a Session journal pointer stub for `panakoes_session_2026-05-12.md` (to be created on next session start). Historical content (night-two log, ADR-009/011/015/019/020/021 long-form sections) preserved verbatim; nothing deleted, supersessions marked inline.
