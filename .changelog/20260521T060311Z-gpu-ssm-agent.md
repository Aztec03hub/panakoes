---
category: Changed
---

- `services/gpu-spawner`: UserData now ensures the SSM agent is enabled + running so the operator can `aws ssm start-session` into the GPU EC2 for `docker exec` + interactive debug without an SSH key. The Deep Learning AMI ships the agent via snap; both unit names tried defensively. The `AmazonSSMManagedInstanceCore` policy was attached to the `gpu-instance` role separately.
