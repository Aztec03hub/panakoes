<script lang="ts">
  // Phase 0 placeholder. Phase 1 wires fetchCostByService() against the
  // cost-api `/cost/by-service` endpoint and renders the typed rows.
  // The `ServiceCostBreakdown` type lives in $lib/types and is consumed
  // by the fetch helper that lands in Phase 1.
</script>

<div class="flex flex-col gap-6">
  <div>
    <h1 class="text-2xl font-bold tracking-tight">Cost by service</h1>
    <p class="text-sm text-muted-foreground">
      Per-service AWS spend over a configurable date window. Wires up in
      Phase 1.
    </p>
  </div>

  <p class="text-sm text-muted-foreground">Coming in Phase 1.</p>
</div>
