(Placeholder CodeQL query selects nothing; the companion scanner `.github/codeql/scripts/scan-tf-secrets.sh` is the real detector. The committed fixture `test-fixtures/tf-secret-default/triggering.tf` cannot carry literal secret-shaped strings because GitHub Push Protection rejects them; values are scrambled with `FIXTURE_*` markers and reassembled at scan time via the procedure documented in `.github/codeql/README.md`. After reassembly, the scanner emits 3 findings against the triggering fixture and 0 against the non-triggering fixture.)
