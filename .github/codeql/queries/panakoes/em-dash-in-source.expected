(Python query. The shipped fixture `test-fixtures/em-dash/triggering.py` contains the U+2014 / U+2013 characters built via `chr()` at runtime, so the literal characters never appear in the source the StrConst extractor sees. To exercise the query against literals, derive `triggering.runtime.py` via the procedure documented in `.github/codeql/README.md`. Expected rows against `.runtime.py`: 2 findings, one per string literal containing the chars.)
